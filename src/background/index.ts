// Background Service Worker (Chrome MV3)
// Reserved for future use (e.g., Phase 09 backend proxy).

chrome.runtime.onInstalled.addListener(() => {
  // No initialization needed at this time.
});

export {};
